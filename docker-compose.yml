services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    volumes:
      - ./server/data:/app/data

  client:
    build: ./client
    ports:
      - "3000:3000"
    depends_on:
      - server